package com.example.globals.enums;

public enum BundleNames {
    TYPE,
    DATA,
    ALARM_ID,
    PERCENTAGE_SAFE_ALARM_VALUE,
    NAME,
    SOUND_ID,
    IS_PERSONAL,
    URI,
    GROUP_ALARM_ID,
    ADD_SINGLE_ALARM_TYPE,
}
